import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hers.pickups',
  appName: 'HERS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
